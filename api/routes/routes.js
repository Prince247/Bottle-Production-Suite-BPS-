// const express = require("express");
// const route = express.Router();
// const allController = require("../index");
// const verifyToken = require("../middleware/middleware");

// const users = allController
// route.post("/signup",users.createUser);
// route.post("/login",users.getUser);

// const despatchorder = allController.despatchorder;
// route.get("/despatchorders",verifyToken,despatchorder.listDespatchOrder);

// module.exports = route;